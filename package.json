{
  "name": "uorqui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:api": "wrangler dev --config wrangler.api.jsonc --port 8787",
    "dev:pages": "wrangler pages dev public --port 8788",
    "deploy:api": "wrangler deploy --config wrangler.api.jsonc",
    "deploy:pages": "wrangler pages deploy public --project-name uorqui",
    "types": "wrangler types --config wrangler.api.jsonc"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
